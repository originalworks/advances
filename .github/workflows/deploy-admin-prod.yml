name: "Deploy Admin Panel to prod"

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-admin:
    uses: ./.github/workflows/deploy-admin.yml
    with:
      environment: 'prod'
    secrets:
      IAM_WORKFLOW_ROLE: ${{ secrets.PROD_IAM_WORKFLOW_ROLE }}